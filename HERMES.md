# Hermes-specific fork of mattpocock/skills

## Upstream sync

```bash
git checkout main
git pull upstream main
git checkout hermes-tweaks
git rebase main
```

Resolve conflicts (rare — only if upstream touched the same lines).
